# SkillPilot Requirements

# Core dependencies
pyyaml>=5.4

# Development dependencies (optional)
pytest>=7.0
pytest-asyncio>=0.21

# Optional for real EDA tool integration
# (not included in install_requires to avoid forcing)
pty>=1.1  # Usually included in Python stdlib, but some distros need it
